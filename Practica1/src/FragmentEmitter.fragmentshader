#version 330 core

//input data:

//in vec2 textureCoords;

// Ouput data:

out vec4 color;

void main(){
	
	//Color plano para el cubo de luz:
	color = vec4(1.0f);
	
	//Aplicar una sola textura como color:
    //color = texture(laTextura, textureCoords); //texture sampler, texture coordinate
}
